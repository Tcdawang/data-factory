server:
  port: 8084

spring:
  profiles:
    active: local
  application:
    name: datafactory-openapi
  cloud:
    nacos:
      config:
        import-check:
          enabled: false

mybatis:
  mapper-locations: classpath*:mapper/**/*.xml
  configuration:
    map-underscore-to-camel-case: true
