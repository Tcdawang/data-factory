server:
  port: 18082

spring:
  profiles:
    active: local
  application:
    name: datafactory-executor
mybatis:
  mapper-locations: classpath*:mapper/**/*.xml
  type-aliases-package: com.datafactory.executor.domain
  configuration:
    map-underscore-to-camel-case: true

management:
  endpoints:
    web:
      exposure:
        include: health,info
